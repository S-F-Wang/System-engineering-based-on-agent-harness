# Align with Pi boundaries without API compatibility

The Reusable Harness will be library-first and will follow Pi's separation between model access, agent runtime, sessions, resources, and user-facing adapters. It will not reproduce Pi's TypeScript names, signatures, package layout, or wire protocols: the project prioritizes a natural Python API and a chapter-by-chapter explanation while retaining the architectural seams that make Pi reusable.
